# v0.9.1 (2026-05-31)

### 修复
- **工具搜索可靠性**：让 ripgrep 路径发现具备平台感知能力，支持 universal bundle，并改进内置 ripgrep 检测，使 `search_code` 在不同环境中更可靠地找到可用二进制文件。 (#42)
- **搜索 Glob 回退**：改进 `search_code` 的回退 glob 处理，使 include/exclude glob 行为在不同平台或运行时存在差异时，搜索仍保持可用。 (#42)
- **编辑文件输出处理**：加固 `edit_file` 输出处理，让 patch 与替换结果更稳定，也更易于 agent 消费。 (#42)

{
  "name": "gateway-vscode",
  "publisher": "three-water666",
  "displayName": "webcode gateway",
  "description": "Connect Web AI to Local VS Code",
  "repository": {
    "type": "git",
    "url": "https://github.com/three-water666/webcode.git"
  },
  "license": "MIT",
  "icon": "images/icon.png",
  "version": "0.9.0",
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "menus": {
      "editor/context": [
        {
          "command": "webcode-gateway.copyContext",
          "when": "editorHasSelection",
          "group": "9_cutcopypaste@100"
        }
      ]
    },
    "commands": [
      {
        "command": "gateway-vscode.restart",
        "title": "MCP: Restart Gateway Server"
      },
      {
        "command": "webcode-gateway.copyContext",
        "title": "webcode: Copy Context"
      }
    ],
    "configuration": {
      "title": "MCP Gateway",
      "properties": {
        "webcodeGateway.aiSites": {
          "type": "array",
          "default": [],
          "description": "User-defined list of AI platforms. Leave empty to use the built-in platform registry managed by the VS Code extension.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Display name"
              },
              "address": {
                "type": "string",
                "description": "URL of the AI service"
              },
              "showQuickLaunch": {
                "type": "boolean",
                "default": true,
                "description": "Show in the main quick pick menu"
              },
              "browser": {
                "type": "string",
                "enum": [
                  "default",
                  "chrome",
                  "edge",
                  "user-profile-chrome",
                  "user-profile-edge",
                  "isolated-chrome",
                  "isolated-edge"
                ],
                "default": "default",
                "description": "Specific browser for this site (overrides global setting). Use user-profile-chrome or user-profile-edge to launch the normal profile with anti-freeze flags when the target browser is closed. Use isolated-chrome or isolated-edge to launch a dedicated profile with the bridge extension and anti-freeze flags."
              },
              "selectors": {
                "type": "object",
                "description": "DOM selectors for this AI site. Leave empty to use defaults for known sites.",
                "properties": {
                  "messageBlocks": {
                    "type": "string"
                  },
                  "codeBlocks": {
                    "type": "string"
                  },
                  "inputArea": {
                    "type": "string"
                  },
                  "sendButton": {
                    "type": "string"
                  },
                  "stopButton": {
                    "type": "string"
                  },
                  "maxInlineChars": {
                    "type": "number",
                    "minimum": 1
                  }
                }
              }
            }
          }
        },
        "webcodeGateway.port": {
          "type": "number",
          "default": 34567,
          "description": "The port the local gateway server will listen on."
        },
        "webcodeGateway.browser": {
          "type": "string",
          "default": "isolated-edge",
          "enum": [
            "default",
            "chrome",
            "edge",
            "user-profile-chrome",
            "user-profile-edge",
            "isolated-chrome",
            "isolated-edge"
          ],
          "description": "Global default browser. Used if no site-specific browser is configured. Edge isolated mode is recommended because it launches a dedicated profile with the bundled bridge extension and anti-freeze flags, so no separate browser extension install is needed. Use regular or user-profile browser modes only after installing the browser extension manually."
        },
        "webcodeGateway.isolatedChrome.executablePath": {
          "type": "string",
          "default": "",
          "description": "Optional path to a Chrome for Testing or Chromium executable for isolated-chrome mode. Google Chrome 137+ no longer supports automatic unpacked extension loading."
        },
        "webcodeGateway.commandShell.path": {
          "type": "string",
          "default": "",
          "description": "Optional path to the POSIX shell used by execute_command and run_in_terminal. On Windows this should point to Git Bash bash.exe. Leave empty to auto-detect Git Bash on Windows or use the user/system POSIX shell on macOS and Linux."
        },
        "webcodeGateway.ripgrep.path": {
          "type": "string",
          "default": "",
          "description": "Optional absolute path to ripgrep for search_code. Leave empty to use VS Code's bundled ripgrep or rg from PATH."
        },
        "webcodeGateway.skillDirectories": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Additional workspace-relative directories to scan for skill folders that contain SKILL.md. These are merged with the built-in default skill directories."
        },
        "webcodeGateway.servers": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "command": {
                "type": "string",
                "description": "Command to execute"
              },
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Arguments"
              },
              "type": {
                "type": "string",
                "enum": [
                  "stdio",
                  "sse",
                  "http"
                ],
                "description": "Connection type",
                "default": "stdio"
              },
              "url": {
                "type": "string",
                "description": "URL for SSE/HTTP"
              },
              "headers": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "description": "HTTP Headers"
              },
              "env": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables"
              },
              "disabled": {
                "type": "boolean",
                "default": false,
                "description": "Disable this server. If true, the server will not be started."
              }
            }
          },
          "default": {},
          "description": "Additional third-party MCP servers. Built-in filesystem and command capabilities are implemented locally by webcode."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "node scripts/prepublish.mjs",
    "compile": "webpack",
    "watch": "webpack --watch",
    "copy-browser-extension": "node scripts/copy-browser-extension.mjs",
    "prepare-browser-extension": "pnpm --filter bridge-browser run build && node scripts/copy-browser-extension.mjs",
    "package": "node scripts/prepublish.mjs",
    "build": "pnpm run package",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run lint",
    "lint": "eslint src",
    "test": "mocha --ui tdd \"out/unit-test/**/*.test.js\"",
    "pretest:extension": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "test:extension": "node scripts/run-extension-tests.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "@webcode/shared": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.106.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.6.0",
    "mocha": "^11.7.5",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.102.1",
    "webpack-cli": "^6.0.1"
  }
}

This conversation has {{PRODUCT_NAME}} attached, providing local file operation capabilities and Skills.
First output the following initialization command to initialize {{PRODUCT_NAME}}. Do not explain it or add any other content. After initialization completes, continue with my request.
The initialization command must be placed inside a JSON code block. Do not use plain text or inline JSON.

```json
{"mcp_action":"call","name":"{{INIT_TOOL_NAME}}"}
```

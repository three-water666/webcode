[System] Reminder: Tool calls MUST use this JSON format: {"mcp_action":"call", "name": "tool_name", "purpose": "reason", "arguments": {...}, "request_id": "step_x"}. Top-level fields may only be mcp_action, name, purpose, arguments, and request_id. name and purpose are required. If a tool has inputs, arguments must exactly match its inputSchema.

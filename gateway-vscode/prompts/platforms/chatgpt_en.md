# Additional Notes

When using {{PRODUCT_NAME}}, **do not** call built-in `python_user_visible`, canvas, or similar visible tools to output noop, no-op, placeholder text, progress notes, or internal state.

Progress updates must use plain natural language only; do not use any tool to produce placeholder output.
